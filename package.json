{
  "name": "masterset",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently --names \"frontend,backend\" --prefix-colors \"cyan,yellow\" \"npm run dev --prefix frontend\" \"python3 backend/api.py\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
